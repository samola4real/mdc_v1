# MDC v1 P3.5 — Provider to Discovery End-to-End Validation

## Status

**COMPLETE.** P3.5 proved the complete controlled pilot path from trusted provider lifecycle write to canonical deployed discovery.

## Goal

Validated:

```text
P3.4 provider lifecycle write
        |
        v
PostgreSQL source of truth
        |
        v
ProviderPublication + CatalogueSyncEvent
        |
        v
DB-backed RDF generation
        |
        v
Fuseki Graph Store synchronization
        |
        v
Fuseki SPARQL query
        |
        v
POST /api/service-discovery/search
```

Controlled provider:

```text
p34_api_validation_provider
```

Controlled offering used for discovery:

```text
p34_api_validation_provider_precision_metal_parts
```

Search characteristics:

```text
service_category=precision_metal_parts
part_family=metal_part
part_type=bracket
```

## Starting P3.5 PostgreSQL state

After P3.4:

```text
providers=5
offerings=8
publications=9
sync_events=11
```

For `p34_api_validation_provider`:

```text
status=active
offerings=2
publications: sync_pending create=1, sync_pending update=3
sync_events: pending=5, attempt_count=0
```

Therefore P3.5 began with real unsynchronized semantic work generated by the deployed lifecycle API.

## Validation strategy

P3.5 first proved the P3.4 provider was absent from the current Fuseki graph and deployed discovery response. It then processed the pending outbox through the trusted local synchronization command and proved the same provider appeared in Fuseki and deployed canonical search.

Primary verification script:

```text
scripts/p35_provider_to_discovery_verify.py
```

## Stage A — local/remote preflight — PASSED

Confirmed:

```text
git status: clean and synchronized
script syntax: PASS
remote Fuseki preflight: PASS
Vercel MDC_CATALOG_SYNC_ENABLED=False: PASS
```

Vercel-side synchronization remained disabled throughout the milestone.

## Stage B — pre-synchronization evidence — PASSED

Before processing the P3.4 outbox:

```text
p34_api_validation_provider absent from Fuseki: PASS
p34_api_validation_provider_precision_metal_parts absent from Fuseki: PASS
p34_api_validation_provider absent from deployed canonical search: PASS
```

This establishes a clear before/after boundary for the synchronization proof.

## Stage C — real synchronization — PASSED

The trusted local operator executed the existing management command with synchronization enabled only for that local process.

Confirmed result:

```text
Catalogue synchronization: selected=4; succeeded=4; failed=0; noop=0; events=5.
```

Therefore all four pending P3.4 publications and all five related outbox events were processed successfully through the real Graph Store synchronization path.

No Vercel-side synchronization was executed.

## Stage D — PostgreSQL durable synchronization evidence — PASSED

Independent read-only verification against `neondb` after the synchronization confirmed:

```text
ProviderPublication for p34_api_validation_provider:
create: synced=1
update: synced=3

CatalogueSyncEvent for those publications:
status=succeeded
events=5
min_attempts=1
max_attempts=1
```

This proves the full P3.4 publication set completed successfully and each related outbox event succeeded exactly once.

## Stage E — semantic/deployed discovery evidence — PASSED

Post-synchronization verification confirmed:

```text
Fuseki triple count: 731
p34_api_validation_provider present in Fuseki: PASS
p34_api_validation_provider_precision_metal_parts present in Fuseki: PASS
deployed canonical search returned p34_api_validation_provider: PASS
contract_version=1.0: PASS
```

Because the P3.4 provider was created through the deployed lifecycle API and was absent from Fuseki/search before synchronization, its later appearance in both Fuseki and deployed canonical discovery is direct end-to-end evidence of the intended MDC flow.

## Synchronization policy retained

Vercel Production remained:

```text
MDC_CATALOG_SYNC_ENABLED=False
```

Semantic writes were performed only from the trusted local operator environment. No public synchronization endpoint was introduced, and no Fuseki Graph Store write credentials were configured for the deployed Vercel runtime.

## P3.5 acceptance

All P3.5 acceptance criteria passed:

- pre-sync verification proved the P3.4 provider was absent from Fuseki and deployed canonical discovery — **PASS**;
- the real pending P3.4 outbox was processed through the existing Graph Store synchronization path — **PASS**;
- PostgreSQL publication/outbox records became `synced` / `succeeded` with no error — **PASS**;
- direct Fuseki SPARQL contains `p34_api_validation_provider` and its controlled metal-parts offering — **PASS**;
- deployed `POST /api/service-discovery/search` returns `p34_api_validation_provider` — **PASS**;
- public contract remains `contract_version=1.0` with no `/api/v1` route — **PASS**;
- PostgreSQL remains the operational source of truth — **PASS**;
- Vercel-side synchronization remained disabled — **PASS**;
- no tracked code changes or secret exposure occurred during validation — **PASS**.

## Milestone result

```text
P3.5 COMPLETE
READY_FOR_P36_AWS_MIGRATION_READINESS_PLAN
```
